experiment:
  name: baseline
  seed: 42

data:
  competition: replace-with-kaggle-slug
  train_file: data/raw/replace-with-kaggle-slug/train.csv
  test_file: data/raw/replace-with-kaggle-slug/test.csv
  text_column: text
  target_column: label
  id_column: id

validation:
  strategy: stratified_kfold
  folds: 5

model:
  name: baseline-to-be-defined

paths:
  model_dir: models/baseline
  output_dir: outputs/baseline
  submission_file: submissions/baseline.csv

